# --- Credenciales y configuración local ---
# Nunca deben subirse: este repositorio es público.
.env
.env.*
!.env.example
config.local.js
secrets.json
*.pem
*.key

# --- Documento de trabajo interno ---
# Se mantiene solo en local: el repositorio es público.
ESTADO.md

# --- macOS ---
.DS_Store
._*
.Spotlight-V100
.Trashes

# --- Editores ---
.vscode/
.idea/
*.swp
*.swo
*~

# --- Herramientas locales ---
.claude/
CLAUDE.local.md

# --- Servicio de guardado ---
# La configuración de Cloudflare y el token nunca se suben.
worker/node_modules/
worker/.wrangler/
.dev.vars

# --- Dependencias y compilación (por si más adelante se agregan) ---
node_modules/
dist/
build/
*.log
